function getBlogsYear(intYear){
	var blogs = "";
	var blogArray = "";
	if(blogMapping != "undefined"){
		for(topicCount = 0; topicCount < blogMapping.length; topicCount++){
			var tempTopicList = new Array();
			if(blogMapping[topicCount].year = intYear){
			}
		}
	}	
	return blogArray;
}
